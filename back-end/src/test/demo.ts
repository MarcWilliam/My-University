import { expect } from 'chai';

/**
 * as always hoping to write unite test but never getting time to do them
 */

describe("first test", () => {
	it("should pass", () => {
		expect(true).to.be.true;
	});
});